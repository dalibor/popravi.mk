- title("Дома")

.main
  %h2 Последни пријави
  - @problems.each do |problem|
    = render :partial => 'problems/problem', :object => problem

#sidebar
  .sidebar
    %h2 Блог
    .blog
      %li
        = link_to @post.title, @post
        %span
          = render 'posts/timestamp', :post => @post
        %p
          = truncate @post.content, :length => 300

  .sidebar
    %h2 Twitter
    #tweets
