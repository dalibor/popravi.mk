- semantic_form_for [:admin, @region] do |f|
  - f.inputs do
    = f.input :name, :label => 'Name'
    = f.input :country, :as => :select, :collection => Country.find(:all, :order => "name ASC")
  - f.buttons do
    = f.commit_button

