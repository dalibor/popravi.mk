= popravi.mk

* http://popravi.mk

== INSTALATION

  - Setup
    gem install bundler
    bundle install
    cp config/database.yml.template config/database.yml
    vim config/database.yml # edit database.yml file
    rake db:create
    rake db:migrate
    rake db:seed

  - Run cucumber features
    cucumber features

  - Run specs
    rspec spec

  - Fast reset
    rake db:drop && rake db:create && rake db:migrate && rake db:seed && rake db:test:prepare

== LICENSE: TODO

Copyright (c) 2010-2011 Dalibor Nasevic
